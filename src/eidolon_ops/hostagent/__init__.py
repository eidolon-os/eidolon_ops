"""The self-contained executor Ops injects into a Host over SSH.

Standard-library only, platform-neutral, and delivered as one payload: nothing
in this package is installed on a product Host. What it may do arrives in the
operator's payload; what it refuses is in ``contract``.
"""
